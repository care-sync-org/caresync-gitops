apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: user-service-pdb
  namespace: {{ .Values.namespace }}
  labels:
    app: user-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: user-service
